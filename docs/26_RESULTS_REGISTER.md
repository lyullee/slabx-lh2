# 26. 결과 등록부

> **논문·보고서·발표에 들어가는 모든 숫자는 `results/results.json` 에서
> 나와야 합니다.** 문서에서 옮겨 적지 마십시오.
>
> ```bash
> python scripts/reproduce.py --json
> ```
>
> §24.10의 자기 오류 다섯 중 셋이 **전사 또는 귀속 오류**였습니다.
> 생성된 값은 그 오류를 가질 수 없습니다.

**등급**은 문서 07의 체계를 따릅니다.

| | |
|---|---|
| **A** | 결정론적. 모델 내부 비교이거나 기준 상관식 대조. 재실행하면 같은 값 |
| **B** | 관측 대조인데 표적이 직접 측정된 길이·시간·이진 사건 |
| **C** | 관측 대조인데 표적이 농도이거나, 시험 수가 적거나, 미측정 입력에 의존 |

---

## 26.1 물 포화 결함

**키** `water_saturation_error`

| T [K] | 현행 [Pa] | IAPWS [Pa] | 비 |
|---|---|---|---|
| 273.16 | 634.2 | 611.7 | 1.0 |
| 260 | 634.2 | 195.8 | **3.2** |
| 240 | 634.2 | 27.27 | **23.3** |
| 200 | 634.2 | 0.1626 | **3,900** |
| 150 | 634.2 | 6.10e-6 | **1.0e8** |

**등급 A.** 확산 자료를 쓰지 않습니다. 기준 상관식(IAPWS) 대조뿐입니다.

**주장 가능**
- *"CoolProp 백엔드가 삼중점 아래에서 포화를 고정하며, 200 K에서 IAPWS 대비 3,900배"*
- *"물의 삼중점이 273.16 K이므로 모든 극저온 구름이 이 구간에 있다"*
- *"실물성 라이브러리로 물 포화를 조회하고 삼중점 아래를 확장하지 않는 모든 확산 코드가 같은 거동을 보인다"*

**주장 불가**
- 다른 코드가 실제로 그렇다는 것 — **확인한 것은 slabx 하나**입니다. 위 세
  번째 문장은 조건문이지 관측이 아닙니다
- 이 결함이 다른 코드의 발표된 결과를 틀리게 했다는 것

**정확한 값이 바뀔 수 있는 곳**: CoolProp 버전. `results.json` 이
`slabx_version` 을 함께 기록합니다.

---

## 26.2 음성 대조

**키** `negative_control_burro8`

| | LFL 거리 [m] |
|---|---|
| 현행 | 447.42 |
| 두 모듈 적용 | 448.62 |
| 관측 | 455 |
| **변화** | **0.27%** |

**등급 A.** 모델 내부 비교입니다. 관측 455 m는 맥락일 뿐 판정에 안 씁니다.

**주장 가능**
- *"Burro 8에서 0.27%"*
- *"LNG 웅덩이 10시험 최대 **0.25%**"* — `negative_control_lng_pool.max_change_pct`.
  **문서 24 §24.1이 0.37%로 적고 있으나 그건 세션 중 손계산이고, 생성값이
  우선입니다**
- *"수동확산(Prairie Grass) 비트 동일"* — `tests/test_integration.py` 가
  매 실행에서 확인

**주장 불가**
- *"검증 결과가 변하지 않았다"* — 0.25%는 **보고 정밀도 아래**이지
  0이 아닙니다. 이 표현을 쓰십시오

---

## 26.3 웅덩이 평형 반경

**키** `pool_radius`

| 사례 | 예측 [m] | 관측 [m] | 비 |
|---|---|---|---|
| E3.5 trial 7 | 0.67 | 0.70 | 0.95 |
| E3.5 trial 13 | 0.95 | 1.20 | 0.80 |
| RR986 test 6 | 0.55 | 0.83 | 0.66 |
| FFI Test 1 | 0.95 | 0.75 | 1.26 |
| FFI Test 7 | 0.83 | 0.75 | 1.11 |
| FFI Test 3 | 1.35 | 0.75 | 1.81 |
| **중앙** | | | **1.03** |

**등급 B.** 표적이 **길이**입니다.

> **주의 — 중앙값이 문서 24에 1.11로 적혀 있습니다.** 그건 NASA를 포함한
> 7개의 중앙이고, `results.json` 의 1.03은 NASA를 뺀 6개의 중앙입니다.
> **논문에는 6개 기준 1.03을 쓰고 NASA는 못에 갇혔다고 각주하십시오.**

**주장 가능**
- *"여섯 개 보고된 웅덩이 반경을 중앙 1.03으로 재현"*

**주장 불가 — 정정**
- *"증발 플럭스가 모델 자신의 지반 모듈에서 나오며 요구값의 97%"* —
  **철회.** 문서 28 참조. 그 97%는 (a) 상온 콘크리트 물성과 (b) 임의로
  고른 t = 60 s 의 조합이었습니다. E3.4가 측정한 극저온 물성으로는
  **t = 60 s 에서 2.48배, 500 s 에서 0.86배, 800 s 에서 0.68배** 이고
  **단일한 비가 없습니다**
- *"자유 파라미터 0"* — `E` 는 적합값은 아니지만 **선택**입니다.
  문헌 1 mm/s 대 E3.4 실측 0.54~0.90 mm/s 사이에서 반경이 0.67~0.91 m 로
  움직입니다 (관측 0.70)

**주장 불가**
- 이걸 확산 소스로 쓸 수 있다는 것 — **`PREREG_lh2_impinging` 이 기각**
- FFI 관측값 0.75 m가 시험별 값이라는 것 — **7시험 전체 범위 "0.5~1.0 m"의
  중점**입니다. 관측 정밀도가 낮습니다

---

## 26.4 LFL 도달거리 — 논문의 핵심 표

**키** `ffi`, `safety_factor`, `lfl_in_bracket`

| trial | 방향 | q [kg/s] | u [m/s] | 관측 구간 | 모델 | ×1.25 | 판정 |
|---|---|---|---|---|---|---|---|
| 1 | 하향 | 0.225 | 3.2 | 0~30 | 25.49 | 31.9 | in |
| 3 | 하향 | 0.730 | 5.8 | 30~50 | 42.32 | 52.9 | in |
| **4** | **수평** | 0.828 | 6.7 | **50~100** | **44.89** | 56.1 | **NOT conservative** |
| 5 | 하향 | 0.715 | 5.2 | 30~50 | 43.56 | 54.5 | in |
| 6 | 수평 | 0.832 | 2.7 | 30~50 | 40.77 | 51.0 | in |
| 7 | 하향 | 0.162 | 6.5 | 0~30 | 26.01 | 32.5 | in |

**등급 B.** 표적이 아크로 묶인 **구간**이지 농도값이 아닙니다.

**주장 가능**
- *"6시험 중 5건이 관측 구간 안"*
- *"실패 1건이 비보수적 쪽이며, 전부 보수화하는 최소 인수가 **1.114**"*
  (`safety_factor_required`)
- *"인수 1.25는 PHMSA가 LNG 안정·저풍속에 의무화한 2.5의 절반"*
- *"RH 60·75·90%에서 결과가 같다"* — `tests/test_integration.py` 가 확인

**주장 불가 — 여기가 가장 위험합니다**
- *"5/6 검증"* — **아닙니다.** 관측 구간이 30~50 m 처럼 넓고, 모델 40.8이
  그 안에 있다는 것은 **±25% 안**이라는 뜻입니다. *"관측이 묶은 구간 안"*
  이라고 정확히 쓰십시오
- *"검증된 안전율 1.25"* — **금지.** 같은 6시험에서 도출하고 같은 6시험으로
  평가했습니다. 정확한 서술: *"최소 필요 인수는 1.114이고, 1.25를 반올림한
  공학적 여유로 검토했다. 독립 검증되지 않았다."*
  그리고 **이 한계가 Discussion 에 들어가야 합니다**

> **격자 아티팩트는 해결됐습니다.** 이전 판에서 trial 3·4·5·6이 모두
> 40.8 m 였던 것은 `slabx` 의 기하급수 격자(비 1.153)에서 **임계 위 마지막
> 격자점**을 돌려줬기 때문입니다. 로그-로그 보간으로 교차점을 계산하며,
> 셀 폭을 절반으로 바꿔도 **0.1% 이내**입니다
> (`tests/test_integration.py::test_lfl_distance_is_grid_independent`).

---

## 26.5 적용범위

**키** `ffi[*].premise_ratio_max`, `critical_wind_ms`

| q [kg/s] | u_crit [m/s] |
|---|---|
| 0.3 | 2.1 |
| 1.0 | 2.5 |
| 3.0 | 3.0 |
| 9.5 | 3.5 |
| 30 | 4.2 |

FFI 6시험 `premise_ratio_max` = 0.02~0.11, 전부 전제 안.
NASA Test 6 = **3.68** (상승각 75°), 전제 밖.

**등급 A** (모델 내부). 다만 `u_crit` 식 자체는 **모델 출력에 대한 적합**이지
관측이 아닙니다.

**주장 가능**
- *"`w_c/u` 가 x 전진 적분의 전제 위반을 진단한다"* — **모델 상태를 직접
  읽는 진단자**
- *"`u_crit = a q^b` 가 유출률과 풍속만으로 사전 선별한다"* — **진단자의
  근사**이지 진단자가 아님
- *"Briggs의 굽은 플룸 공식도 같은 전제 위에 있어 그 영역에서 무효"*
- *"실행 시 `applicability()` 가 VALID / MARGINAL / OUT_OF_SCOPE 와 사유를
  돌려준다"* — 모니터링 적용의 인터페이스

**주장 불가**
- *"u < u_crit 에서 실제 구름이 이렇게 거동한다"* — 이건 **모델의 유효성
  경계**이지 물리 현상의 경계가 아닙니다
- 안정도 A~C·E·F 에서의 `u_crit` — D에서만 적합했습니다. 세션 중 B/F 값을
  계산했지만 `results.json` 에 없습니다

---

## 26.6 Briggs 리프트오프

**키** `ffi[*].briggs_Lp_max`

| trial | u [m/s] | u* | L_p | 100 m 관측 |
|---|---|---|---|---|
| 6 | 2.7 | 0.160 | **28.9** | **미검출** |
| 1 | 3.2 | 0.190 | 11.7 | 검출 |
| 5 | 5.2 | 0.309 | 6.5 | 검출 |
| 3 | 5.8 | 0.345 | 4.9 | 검출 |
| 7 | 6.5 | 0.386 | 2.3 | 검출 |
| 4 | 6.7 | 0.398 | 3.8 | 검출 |

**등급 C.** 6시험, 그리고 FFI는 이 가설을 **동기부여**한 자료라 검정이
아닙니다.

**주장 가능**
- *"`u*²` 를 분모로 쓰면 네 자료원(FFI·E3.5·RR986·NASA)에서 정렬이 맞는다"*
  — E3.5 순위상관 −0.891, NASA −1.000
- *"임계 20이 척도를 넘지 않는다"* — 0.07~0.83 kg/s 세 자료원에서 가르고,
  10~17 kg/s NASA에서 네 시험 전부 초과
- *"게이트가 아니라 진단자로 쓴다"*

**주장 불가**
- *"리프트오프를 예측한다"* — **채택하지 않았습니다.**
  `PREREG_lh2_briggs_liftoff` 가 P-B2·P-N4 실패로 미채택
- E3.5·NASA 순위상관은 `results.json` 에 **없습니다**.
  `docs/prereg/PREREG_lh2_briggs_liftoff.md` 의 RESULTS 절에 있고, 논문에
  쓰려면 그 계산을 스크립트로 옮겨야 합니다

---

## 26.7 `results.json` 에 아직 없는 것

| 값 | 상태 |
|---|---|
| LNG 10시험 전체 | ✅ `negative_control_lng_pool` — **최대 0.25%** |
| NASA 4시험 상승 지수·잔차 분해 | ✅ `nasa` |
| P-W1 / P-W2 통과 수 | ✅ `prereg_plume_width` — **0/4, 2/4** |
| 지반 전도 대 문헌 증발률 | ✅ `ground_flux` — 비 **0.974** |
| 필요 최소 보수화 인수 | ✅ `safety_factor_required` — **1.114** |
| E3.5 순위상관 | ✅ `briggs_e35` — 풍속 **−0.891**, 유출률 **+0.213** (n=17) |
| 안정도별 `u_crit` | ✅ `critical_wind` — **실측 재적합**. 아래 정정 참조 |
| 공기 응축 개시 | ✅ `air_condensation` — N₂ **90.27 mol%**, O₂ **88.15 mol%** |

**목록이 비었습니다.** 논문에 들어갈 모든 숫자가
`scripts/reproduce.py --json` 에서 재생성됩니다.

### 정정 — `u_crit` 계수가 바뀌었습니다

문서 22가 `u_crit ≈ 2.5 q^0.15` (안정도 D)로 적었습니다. 안정도별로 정식
재적합하니 **D는 `2.674 q^0.135`** 입니다. 0.1~30 kg/s 구간에서 두 식의
차이는 3% 이내이지만, **논문에는 재적합값을 쓰십시오.**

| class | a | b | u_crit @ 9.5 kg/s |
|---|---|---|---|
| A | 1.878 | 0.118 | 2.45 |
| B | 1.968 | 0.121 | 2.58 |
| C | 2.147 | 0.124 | 2.84 |
| **D** | **2.674** | **0.135** | **3.62** |
| E | 3.839 | 0.129 | 5.13 |
| F | 4.848 | 0.119 | 6.34 |

**적합 구간은 0.1~30 kg/s 입니다.** Zhang의 53 kg/s는 외삽이고,
`CRITICAL_WIND_RANGE_KGS` 가 그 범위를 들고 있습니다.

**주장 불가**
- 이것을 **실험 상관식**처럼 쓰는 것. `w_c/u` 는 모델 상태를 직접 읽는
  **진단자**이고, `u_crit` 는 그 진단자의 **사전 선별 근사**입니다.
  둘을 같은 지위로 쓰면 안 됩니다

## 26.7a 상승 지수 — 문서 24와 어긋납니다

문서 24 §24.1이 *"상승 지수 1.220 → 0.869"* 로 적고 있습니다.
**그건 물 승화 수정 이전 값입니다.** 두 모듈을 함께 켠 생성값:

| test | baseline | water only | width only | **both** |
|---|---|---|---|---|
| 2 | 1.283 | 1.336 | 1.260 | **1.275** |
| 6 | 1.236 | 1.284 | 0.904 | **1.068** |
| 4 | 1.188 | 1.281 | 0.891 | **1.053** |
| 5 | 1.137 | 1.233 | 0.923 | **1.055** |

**P-W1 (0.6~0.9) 0/4, P-W2 (0.5~2.0) 2/4.**

**주장 불가**
- *"폭 결합이 굽은 플룸 스케일링을 회복한다"* — 물 수정과 함께 켜면 아닙니다
- 1.220 → 0.869 을 인용하는 것 — **철회됐습니다**
  (`prereg/PREREG_lh2_plume_width.md` ADDENDUM)

**주장 가능 — 잔차가 정량화됩니다**

`n = 2/(s+1)`, `s = dlnB/dlnz + dlnh/dlnz`. Briggs는 `s = 2`.

| test | dlnB/dlnz | dlnh/dlnz | s | 2/(s+1) | 실제 n |
|---|---|---|---|---|---|
| 2 | 0.390 | 0.229 | 0.618 | 1.236 | 1.275 |
| 6 | 0.470 | 0.348 | 0.818 | 1.100 | 1.068 |
| 4 | 0.592 | 0.421 | 1.013 | 0.994 | 1.053 |
| 5 | 0.521 | 0.492 | 1.013 | 0.994 | 1.055 |

**네 시험 전부 4% 이내로 맞습니다.** 즉:
- *"깊이가 상승에 전혀 결합돼 있지 않다"* (dlnh/dlnz 0.23~0.49, 필요 1.0)
- *"폭은 절반만 결합됐다"* (0.39~0.59) — EQ 7의 중력확산·지면유입 항이 희석
- **누락 항이 특정됐고 크기가 재어졌다**

**그리고 NASA 4시험 전부 `w_c/u` = 0.61~4.74 로 적용범위 밖입니다.**
전제 안에서 뜬 구름의 상승을 잰 자료가 없습니다.

---

## 26.8 절대 쓰면 안 되는 표현

| 쓰지 말 것 | 이유 | 대신 |
|---|---|---|
| *"검증되었다"* | 6시험, 구간 표적, 등급 B~C | *"관측이 묶은 구간 안에 들어온다"* |
| *"SLAB을 LH2로 확장했다"* | 확장이 아니라 **결함 하나와 결합 하나** | *"극저온 열역학 결함을 고치고 상승 결합을 추가했다"* |
| *"밀도가스 결과가 변하지 않았다"* | 0.37%는 0이 아님 | *"보고 정밀도 아래에서 불변"* |
| *"리프트오프를 예측한다"* | 미채택 | *"리프트오프 경향을 정렬한다"* |
| *"NASA Test 6에서 20 m 분리"* | **Ichard의 등고선 판독.** 원본 관측은 지면 이동 50~100 m | 둘을 분리해서 각각 출처 명시 |
| *"방출 방향을 구별하지 못한다"* | 하향은 이미 맞음 | *"수평 고운동량 제트의 최대값을 과소예측한다"* |

**마지막 둘이 이 세션에서 제가 실제로 틀렸던 것들입니다.**

---

## 26.9 논문 그림·표 계획

| | 내용 | 출처 키 | 등급 |
|---|---|---|---|
| **Fig 1** | 물 포화: 현행·IAPWS·legacy 세 곡선, 150~280 K | `water_saturation_error` | A |
| **Fig 2** | 적용범위 지도: q–u 평면, 안정도별 `u_crit` 곡선. FFI 안, NASA 경계~밖, **Zhang 강하게 밖**, E3.5 표시. 점의 색을 `max w_c/u` 로 | `critical_wind`, `ffi`, `nasa`, `zhang` | A |
| **Table 1** | 시험 조건 (FFI 6시험) | `ffi` | — |
| **Table 2** | **LFL 거리 대 관측 구간** ← 핵심 | `ffi` | B |
| **Table 3** | 음성 대조 (LNG 10 + 수동확산) | 확장 필요 | A |
| **Fig 3** | Briggs `L_p` 대 풍속, 네 자료원 | 확장 필요 | C |
| Fig 4 | 실시간 적용 개념도 (모니터링) | — | — |

**Fig 2가 논문의 얼굴입니다.** 모델 결과가 아니라 **모델이 언제 답하면
안 되는지의 지도**이고, 그게 이 작업의 novelty입니다.
